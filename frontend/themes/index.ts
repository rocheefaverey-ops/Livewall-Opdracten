import base from './styles/base';
import extraTheme from './styles/extra-theme';
import { IThemes } from './utils/extend';

export const DEFAULT_THEME = 'base';

export const themes: IThemes = {
  base,
  extraTheme
};
